import { Profile } from "../models/profile.model";

export const isUserVerified = async (req: any, res: any, next: any) => {
    // const profile = await Profile.findOne({ user: req.user }).exec()
    // console.log(profile)

    // next();
}